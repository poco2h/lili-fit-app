export type Profesional = {
  slug: string;
  nombre: string;
  especialidad: string;
  ciudad: string;
  bio: string;
  precioTextoMin: number; // € / sesión 20 min texto — lo fija el propio profesional
};
